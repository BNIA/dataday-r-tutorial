+subsection('Vectors')(id="vectors")
	p
		| A vector is an indexed set of values. This is also known as an array, or a list in other languages. The vector is the most primitive type in R. That is, the most basic elements in R are all vectors. For example:
	+rcode
